"use client";

import React from "react";
import Nav from "./navigation";

const Header = () => {
  return (
    <header className="ly-header">
      <Nav />
    </header>
  );
};

export default Header;
