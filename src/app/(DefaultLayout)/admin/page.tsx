'use client'
import React, { useState } from 'react'
import { SideBar } from '@/components'

const adminPage = () => {
  return(
    <div>
      <SideBar />
    </div>
  )
}

export default adminPage;